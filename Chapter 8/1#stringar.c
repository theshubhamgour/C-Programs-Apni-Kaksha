#include <stdio.h>
//IN string \0 is the last character which is compulsory
int main()
{
    //char name[] = {'S', 'H', 'U', 'B', 'H', 'A', 'M', '\0'};
    char name2[] = {"Shubham"};
    printf(name2);
    //printf(name);
    return (0);
}